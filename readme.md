# 📊 Exact MVA - Chapter 6 Implementation

Este projeto implementa o algoritmo **Exact Mean Value Analysis (MVA)** para sistemas fechados multiclasses, conforme apresentado no **Capítulo 6** do livro:

> Menascé, D. A., Almeida, V. A., & Dowdy, L. W.  
> *Capacity Planning and Performance Modeling: From Mainframes to Client-Server Systems*. Prentice Hall, 1994.


The project focuses on the implementation of queueing network performance analysis techniques, including Exact MVA, Approximate MVA, and Mixed MVA models.

The `reference_materials/` directory contains the reference pseudocode used during the implementation process, as well as example models, input data, and expected results extracted from the reference materials.

The `src/` directory contains the Python implementations of the algorithms. Each implementation already includes the test input values used in the reference examples, allowing direct execution and validation of the generated results.

The `results/` files store the final outputs generated by the algorithms for comparison with the reference material results.

---

## 📁 Estrutura do Projeto

```text
multi-class-performance-models/
│
├── reference_materials/
│   ├── approximate_mva/
│   │   ├── approximate_algorithm.png
│   │   ├── model_data.png
│   │   └── results.png
│   │
│   ├── exact_mva/
│   │   ├── exact_algorithm.png
│   │   ├── model_data.png
│   │   └── results.png
│   │
│   └── mixed_mva/
│       ├── model.png
│       ├── model_data.png
│       └── results.png
│
├── src/
│   ├── results/
│   │   ├── approximate_mva.txt
│   │   ├── exact_mva.txt
│   │   └── mixed_mva.txt
│   │
│   ├── approximate_mva.py
│   ├── exact_mva.py
│   └── mixed_mva.py
│
├── requirements.txt
├── README.md
└── .gitignore